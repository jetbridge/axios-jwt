name: Build on: push: branches: - 'master' pull_request_target: branches: - 'master' jobs: build: runs-on: ubuntu-latest steps: - uses: actions/checkout@v2 - uses: actions/setup-node@v2 with: node-version: '14.x' - name: 'Install Dependencies' id: install run: yarn - name: 'Build Project' id: build run: yarn build
